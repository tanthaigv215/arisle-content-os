import { ModulePage } from "@/components/ui/ModulePage";

export default function SettingsPage() {
  return (
    <ModulePage
      title="Settings"
      description="Configure brand presets, integrations, permissions, and system preferences."
      moduleName="Settings"
    />
  );
}
